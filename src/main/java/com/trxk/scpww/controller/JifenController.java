package com.trxk.scpww.controller;

import org.springframework.stereotype.Controller;

@Controller
public class JifenController
{
   /* @Autowired
    JiFenService jiFenService;
    Map<String ,String> map = new HashMap<>();
    @RequestMapping(value = "" ,method = RequestMethod.POST)
    @ResponseBody
    public Map<String,String> insertUser(@RequestBody(required = false) User user){
        if(jiFenService.insertUser(user)){
            map.put("code","200");
            map.put("msg","插入成功");
            return map;
        }else {
            map.put("code","200");
            map.put("msg","插入失败");
            return map;
        }
    }
*/

}
